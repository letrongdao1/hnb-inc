{
  "buildCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"prod\" ]; then npm run build; else echo '⛔️ Build skipped: not prod branch.' && exit 0; fi",
  "outputDirectory": "out",
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"prod\" ]; then echo 'Skipping deploy for non-prod branch'; exit 0; fi"
}
